service:
  type: ClusterIP
apiService:
  create: true
{{ if eq .Values.clusterType "minikube" }}
args:
  - --kubelet-insecure-tls
{{ end }}
resources:
  limits:
    cpu: 40m
    memory: 40Mi
  requests:
    cpu: 40m
    memory: 40Mi

# --metric-resolution=15s by default here