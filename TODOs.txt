Data Eng:

1. Plot available time ranges (Athena SQL + wrangler + plotly/matplotlib)
	a. Figure out group by versions
2. Move df configs from Mysql to Athena
	a. Refactor cryptostore fork
	b. Move existing mysql records to Athena
2. Monitor available timeranges/data size/bucket files count/frequency on S3
	a. Athena SQL + Prefect job to Prom
		or
	b. Athena SQL + some AWS monitoring tool
	    or
	c. Athena SQL + lambda + metric to Prom/or cloudwatch alert?
3. Prefect pipelines setup
	a. ECS agent
	b. Dockerize pipeline
	c. Figure out Dask
4. Prefect data validation pipeline with Pandera
5. Prefect pipeline to move data from old tables to new market_data_table
6. Prefect compaction pipeline

Featurizer:

1. Figure out abstract class to be used offline with Dask and Athena/s3 + online with Kuber/backtest/data-feed + Prefect validation via online to offline conversion, distributed
	a. Create MVP mid-price feature (nbbo)
	b. Create MVP volatility feature
	c. How to concat different ranges?
	d. Versioning?

Backtest:
1. Figure out architecture (Kuber + data-feed + feature-service + Ray(Tune, RL, Serve) + MLFlow)

